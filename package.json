{
  "name": "root",
  "devDependencies": {
    "lerna": "^5.1.4"
  },
  "scripts": {
    "test": "lerna run test",
    "test-specific": "lerna run test --scope=$scope",
    "diff": "lerna diff",
    "start": "lerna run start",
    "clean": "npx lerna clean -y",
    "hoist": "npx lerna bootstrap --hoist",
    "new-version":"lerna version --conventional-commits --yes"
  }
}
